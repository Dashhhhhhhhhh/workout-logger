<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Workout Logger</title>
</head>
<body>
    <h1>Create a workout program: </h1>
    <form action="/add-workout" method="POST">
        <input type="text" name="exercise" placeholder="Exercise Name" required />
        <input type="number" name="sets" placeholder="Number of Sets" required />
        <input type="number" name="reps" placeholder="Number of Reps" required />
        <input type="date" name="date" required>
        <button type="submit">Add Exercise</button>
    </form>
    <form action="/clear-workouts" method="POST">
    <button type="submit">Clear All Workouts</button>
    <label for="date">Date:</label>
    </form>
</form>

<% if (workouts.length > 0) { %>
  <h2>All Tasks:</h2>
  <ul>
  <% workouts.forEach((e, index) => { %>
    <li>
      <strong><%= e.exercise %></strong> -
      <%= e.sets %> sets × <%= e.reps %> reps -
      <%= e.date ? new Date(e.date).toLocaleDateString() : "No date" %>
      <form action="/delete-workout/<%= index %>" method="POST" style="display:inline;">
        <button type="submit">Delete</button>
      </form>
    </li>
  <% }) %>
  </ul>
<% } %>
    
</body>
</html>